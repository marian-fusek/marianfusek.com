import{prepareFold,playFold}from'./fold-text.js';export function initHeresy(items){const title=document.querySelector('.heresy-intro h2');prepareFold(title);const ti=new IntersectionObserver(([e])=>{if(e.isIntersecting){playFold(title,{stagger:.05});ti.disconnect()}},{threshold:.4});ti.observe(title);const host=document.getElementById('heresyList');items.forEach((x,i)=>{const s=document.createElement('section');s.className='heresy-item';s.innerHTML=`<button class="heresy-trigger"><span class="heresy-no">${String(i+1).padStart(2,'0')}</span><span class="heresy-title">${x[0]}</span></button><div class="heresy-body"><div><p>${x[1]}</p></div></div>`;s.querySelector('button').onclick=()=>s.classList.toggle('open');host.appendChild(s);const io=new IntersectionObserver(([e])=>{if(e.isIntersecting){setTimeout(()=>s.classList.add('visible'),i*55);io.disconnect()}},{threshold:.12});io.observe(s)})}
