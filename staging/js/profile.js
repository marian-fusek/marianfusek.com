import{prepareFold,playFold}from'./fold-text.js';export function initProfile(data){const panel=document.getElementById('profilePanel');const buttons=[...document.querySelectorAll('[data-profile]')];function render(key){const d=data[key];panel.innerHTML=`<p class="profile-lead fold-target"></p>${d.small.map(x=>`<p class="profile-small">${x}</p>`).join('')}<div class="profile-note">${d.note}</div>`;const lead=panel.querySelector('.profile-lead');lead.textContent=d.lead;prepareFold(lead,{words:true});playFold(lead,{words:true,stagger:.025});buttons.forEach(b=>b.classList.toggle('active',b.dataset.profile===key))}buttons.forEach(b=>b.onclick=()=>render(b.dataset.profile));render('101')}
